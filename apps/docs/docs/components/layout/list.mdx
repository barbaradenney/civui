---
title: List
sidebar_position: 5
sidebar_label: List
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import ListProps from "./_list.props.mdx";
import ListItemProps from "./_list-item.props.mdx";

# List

<!-- contract-link:start -->

> **Contract reference:** [`civ-list`](pathname:///civui/storybook/?path=/docs/contract-civ-list--docs) — auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-list>` and `<civ-list-item>`

Generic list primitives. Use for any vertical row collection — side navigation, settings menus, link collections, search results, and the **task list pattern** for multi-chapter government forms.

`<civ-list-item>` becomes a clickable anchor when `href` is set, and a static row otherwise. The host element carries `role="listitem"` so the parent `<civ-list>`'s `<ul role="list">` accessibility tree sees a proper list item. Both variants render with the same layout, so clickable and locked rows can sit in the same list with consistent visual rhythm.

## Examples

### Simple navigation list

<StoryEmbed id="layout-list--default" />

### With dividers

<StoryEmbed id="layout-list--with-dividers" />

### With icons (start slot)

<StoryEmbed id="layout-list--with-icons" />

### Heading and description

<StoryEmbed id="layout-list--heading-and-description" />

### Heading + start icon + end badge

<StoryEmbed id="layout-list--heading-with-start-and-end" />

### Rich heading slot (custom heading levels)

<StoryEmbed id="layout-list--rich-heading-slot" />

### Rich description with links

<StoryEmbed id="layout-list--rich-description-slot" />

### Mixed clickable and static rows

<StoryEmbed id="layout-list--mixed-clickable-and-static" />

### Side nav with current page

<StoryEmbed id="layout-list--side-nav-with-current" />

### Compact spacing

<StoryEmbed id="layout-list--compact-spacing" />

### Nested list

<StoryEmbed id="layout-list--nested-list" />

### Nested task list (chapters with sections)

<StoryEmbed id="layout-list--nested-task-list" />

### With trailing badges

<StoryEmbed id="layout-list--with-trailing-badges" />

### With action buttons (end slot)

<StoryEmbed id="layout-list--with-action-buttons" />

### With errors

<StoryEmbed id="layout-list--with-errors" />

### Density scale

<StoryEmbed id="layout-list--density-scale" />

### Government task list

<StoryEmbed id="layout-list--government-task-list" />

### Government side navigation

<StoryEmbed id="layout-list--government-side-nav" />

For the **task list pattern** (status badges, sequential unlocking), see the
[Task List Hub](/form-patterns/task-list-hub) pattern guide.

[Open in Storybook →](pathname:///civui/storybook/?path=/story/layout-list--default)

## civ-list Props

<ListProps />

## civ-list-item Props

<ListItemProps />

## Slots

`<civ-list-item>` has five slot zones:

| Slot | Attribute | Description |
|------|-----------|-------------|
| **start** | `data-list-item-start` | Leading content (icons, avatars, thumbnails) |
| **heading** | `data-list-item-heading` | Rich heading — any `<h2>`-`<h6>`, links, custom styles. Overrides `heading` prop. |
| **description** | `data-list-item-description` | Rich description — links, bold, HTML. Overrides `description` prop. |
| **(default)** | — | Body content |
| **end** | `data-list-item-end` | Trailing content (badges, action buttons) |

## Task list pattern

The "task list" is a usage pattern, not a dedicated component. Compose `<civ-list>` + `<civ-list-item>` + `<civ-badge>`. Setting `href` on a list item makes the whole row clickable; omit `href` for locked rows.

`<civ-badge>` carries `role="status"` and the `with-icon` attribute auto-renders the variant's semantic icon (success → check-circle, error → ✕, warning → ⚠).

| Status | Badge label | variant | badge-style |
|--------|-------------|---------|-------------|
| not-started | Not started | `info` | `secondary` |
| in-progress | In progress | `info` | `primary` |
| complete | Complete | `success` | `primary` |
| cannot-start | Cannot start yet | `neutral` | `secondary` |
| error | Has errors | `error` | `secondary` |
| review | Needs review | `warning` | `primary` |

```html
<h3 class="civ-heading-md">Fill out your application</h3>
<civ-list dividers>
  <civ-list-item href="/personal">
    <span class="civ-block civ-font-bold">Personal information</span>
    <span class="civ-block civ-text-sm civ-text-muted">Name, date of birth, Social Security number</span>
    <civ-badge data-list-item-end label="Complete" variant="success" badge-style="primary" with-icon></civ-badge>
  </civ-list-item>
  <civ-list-item href="/contact">
    <span class="civ-block civ-font-bold">Contact information</span>
    <civ-badge data-list-item-end label="In progress" variant="info" badge-style="primary" with-icon></civ-badge>
  </civ-list-item>
  <civ-list-item>
    <span class="civ-block civ-font-bold">Service history</span>
    <civ-badge data-list-item-end label="Cannot start yet" variant="neutral" badge-style="secondary" with-icon></civ-badge>
  </civ-list-item>
</civ-list>
```

See the [Task List Hub pattern](/form-patterns/task-list-hub) for a full multi-chapter walkthrough.

## Side navigation pattern

```html
<civ-list>
  <civ-list-item href="/dashboard">Dashboard</civ-list-item>
  <civ-list-item href="/claims" current>Claims</civ-list-item>
  <civ-list-item href="/messages">Messages</civ-list-item>
  <civ-list-item href="/settings">Settings</civ-list-item>
</civ-list>
```

## Accessibility

- Each clickable row is a real `<a>` filling the row — works with keyboard, screen readers, and right-click "open in new tab".
- Static rows render as a plain row with no anchor. Same visual rhythm, honest semantics — assistive tech doesn't announce them as links.
- `<civ-list>` adds `role="list"` on its `<ul>` and `<civ-list-item>` carries `role="listitem"` on the host. The custom element doesn't render its own `<li>` (that would break the HTML spec, which requires `<li>` to be a direct child of `<ul>`/`<ol>`), but the accessibility tree still sees a proper list-item child via the role.
- `current` on `<civ-list-item>` sets `aria-current="page"` for navigation patterns.

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/list/`. Include it from any Twig template — the SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:list' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`list.component.yml`](pathname:///civui/storybook/?path=/story/layout-list-drupal-sdc--default) and the template in [`list.twig`](pathname:///civui/storybook/?path=/story/layout-list-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="layout-list-drupal-sdc--default" />

