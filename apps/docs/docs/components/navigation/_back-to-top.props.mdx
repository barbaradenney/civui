{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `label` | `string` | `'Back to top'` | Accessible name for the button. Also shown as a tooltip via `title` |
| `threshold` | `number` | `400` | Pixels scrolled before the button appears |
| `hidden` | `boolean` | `true` | **Internal state — treat as read-only.** Controlled by the component's `IntersectionObserver` (flips to `false` when the user scrolls past `threshold`, `true` when they scroll back up). Reflected so the global `[hidden]` HTML rule applies `display: none`. Consumers should NOT set this prop directly — writes are overwritten on the next scroll. Native implementers should expose visibility as derived state from their own scroll listener rather than as a writable input. |
