<civ-disclosure
  {% if label %}label="{{ label }}"{% endif %}
  {% if open %}open{% endif %}
  {% if icon is not null %}icon="{{ icon }}"{% endif %}
  {% if size %}size="{{ size }}"{% endif %}
>{% block content %}{% endblock %}</civ-disclosure>
