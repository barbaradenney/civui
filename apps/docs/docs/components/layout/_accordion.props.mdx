{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `single` | `boolean` | `false` | When true, opening one item collapses any other open siblings. Default behavior (omitted) allows multiple items open at once. The single-open invariant is enforced on first paint AND when this prop transitions from false to true at runtime. |
| `disabled` | `boolean` | `false` | Disables every direct-child item. Each item checks the nearest ancestor accordion's `disabled` when deciding whether to accept a toggle; setting `disabled` here cascades to all items without mutating their own `disabled` prop. Individual items can also be disabled independently. |
| `variant` | `'primary'` \| `'secondary'` \| `'tertiary'` | `'tertiary'` | Visual variant. `tertiary` (default) is a bordered group with transparent triggers — list-like, quiet. `secondary` renders each item as a filled neutral-gray button using the same base-lightest chip palette as `civ-disclosure` and `civ-read-more`, with the smaller chrome of `civ-action-btn--secondary`. `primary` renders as a filled primary-lightest (blue) button with the larger padding and bolder type of `civ-btn--secondary`. Items in primary / secondary stack with a small gap; no outer border or inter-item dividers. |
