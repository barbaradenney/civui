{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `type` | `'text'` \| `'email'` \| `'number'` \| `'password'` \| `'search'` \| `'tel'` \| `'url'` | `'text'` | HTML input type controlling keyboard and secure entry |
| `width` | `'default'` \| `'2xs'` \| `'xs'` \| `'sm'` \| `'md'` \| `'lg'` \| `'xl'` \| `'2xl'` | `'default'` | Width variant |
| `spacing` | `'default'` \| `'sm'` | `'default'` | Density variant. `default` renders the full label / hint / error chrome. `sm` (compact) renders just the bare `<input>` with the host's `aria-label` propagated for screen-reader text. For use in dense surfaces like data-grid cell editors where the surrounding context labels the control. Mask / validation behavior is preserved in compact mode |
| `placeholder` | `string` | `''` | Placeholder text (never use as sole label) |
| `pattern` | `string` | `''` | Validation regex pattern |
| `maxlength` | `number` | — | Maximum character length |
| `minlength` | `number` | — | Minimum character length |
| `hide-char-count` | `boolean` | `false` | When maxlength is set, suppresses the visual character counter and aria-live announcements |
| `autocomplete` | `string` | `''` | Autocomplete hint |
| `inputmode` | `string` | `''` | Input mode hint for virtual keyboards |
| `mask` | `''` \| `'ssn'` \| `'phone-us'` \| `'zip'` \| `'zip4'` \| `'ein'` \| `'currency'` | `''` | Built-in mask preset (blur-mode formatting). Empty string means no mask |
| `mask-pattern` | `string` | `''` | Custom mask pattern: # = digit, A = letter, * = any printable char. Other chars are literal inserts |
| `mask-mode` | `'blur'` \| `'live'` | `'blur'` | Mask formatting mode. blur (default) is accessible; live formats as user types |
| `validate` | `''` \| `'email'` \| `'phone'` \| `'phoneIntl'` \| `'ssn'` \| `'ein'` \| `'routing'` \| `'zip'` \| `'zip4'` \| `'usState'` \| `'url'` \| `'currency'` \| `'alphanumeric'` | `''` | Declarative validation preset. Auto-validates on blur. Empty string means no validation |
| `prefix` | `string` | `''` | Inline prefix label rendered to the left of the input (e.g. "$"). Mutually exclusive with leadingIcon |
| `suffix` | `string` | `''` | Inline suffix label rendered to the right of the input (e.g. ".com"). Mutually exclusive with trailingIcon and the clear button |
| `clearable` | `boolean` | `false` | When true and the value is non-empty, renders a clear button on the trailing edge |
| `reveal-password` | `boolean` | `false` | When true and type="password", renders a Show / Hide toggle in the helper row below the input that flips the rendered input type between "password" and "text". The host's `type` prop stays `password` so the field's semantic identity does not change. Suppressed for non-password types. Coexists with `clearable` — clear stays inset, reveal sits below |
| `icon-start` | `string` | `''` | Icon rendered inside the input on the leading edge. Any civ-icon name. Ignored when prefix is set |
| `icon-start-label` | `string` | `''` | Accessible label for the leading icon. When omitted, icon is decorative (aria-hidden) |
| `icon-end` | `string` | `''` | Icon rendered inside the input on the trailing edge. Ignored when suffix is set or the clear button is showing |
| `icon-end-label` | `string` | `''` | Accessible label for the trailing icon. When omitted, icon is decorative |
| `decimals` | `number` | `2` | Currency-mask only: decimal places to keep ("1,234.56" with 2, "1,234" with 0). `0` is whole-dollar mode for tax-style fields (W-4 line 4c, VA benefits) |
| `min` | `number` | — | Currency-mask only: minimum allowed amount. Out-of-range values surface an inline error on blur |
| `max` | `number` | — | Currency-mask only: maximum allowed amount. Out-of-range values surface an inline error on blur |
| `allow-negative` | `boolean` | `false` | Currency-mask only: accept negative amounts (refunds, adjustments). Standard mode rejects values below zero |
