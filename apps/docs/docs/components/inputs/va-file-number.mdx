---
title: VA File Number
sidebar_position: 21
sidebar_label: VA File Number
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# VA File Number

`<civ-va-file-number>`

Pre-configured input for VA file numbers with built-in validation and plain-language labeling.

## Examples

### Default

<StoryEmbed id="inputs-va-file-number--default" />

### Custom Label

<StoryEmbed id="inputs-va-file-number--custom-label" />

### With Error

<StoryEmbed id="inputs-va-file-number--with-error" />

### Disabled

<StoryEmbed id="inputs-va-file-number--disabled" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/inputs-va-file-number--default)

## Shared Props

All form components share these props: `label`, `name`, `value`, `hint`, `error`, `required`, `disabled`, `readonly`.

The label defaults to "VA file number" and can be overridden via the `label` prop.

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `civ-input` | `{ value: string }` | Fires on every value change |
| `civ-change` | `{ value: string }` | Fires on committed value change |

## Usage

```html
<!-- VA file number input with built-in validation -->
<civ-va-file-number name="vaFileNumber" required></civ-va-file-number>
```
