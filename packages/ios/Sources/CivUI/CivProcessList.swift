// CivUI — CivProcessList for SwiftUI
// Numbered preview list of upcoming steps. Mirrors civ-process-list.
//
// Placeholder body — the prop surface satisfies schema parity. The
// SwiftUI implementation should render a VStack of CivProcessListItem
// rows with auto-incremented step numbers from ForEach indices. See
// .claude/rules/audit-debt.md → "Native platform implementation pass".

import SwiftUI

public struct CivProcessList: View {
    public init() {}

    public var body: some View {
        EmptyView()
    }
}
