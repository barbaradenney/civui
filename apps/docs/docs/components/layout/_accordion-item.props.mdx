{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `label` | `string` | `''` | Header text rendered on the full-width trigger row |
| `open` | `boolean` | `false` | Whether the panel is currently expanded |
| `disabled` | `boolean` | `false` | Disables interaction; the row is dimmed and not toggleable |
| `heading-level` | `number` | — | Wrap the label in an `<h1>`–`<h6>` for screen-reader rotor navigation. Accepts integers 1–6; values outside that range fall back to a plain `<span>` and trigger a dev-mode warning. Visual treatment stays constant across all levels — the heading element only affects assistive-tech navigation. Web-only; native platforms expose disclosure semantics through their own header conventions. |
