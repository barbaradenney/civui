---
title: Drawer
sidebar_position: 12
sidebar_label: Drawer
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_drawer.props.mdx";
import EventsTable from "./_drawer.events.mdx";

# Drawer

<!-- contract-link:start -->

> **Contract reference:** [`civ-drawer`](pathname:///civui/storybook/?path=/docs/contract-civ-drawer--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-drawer>`

A side-anchored full-viewport-height dialog that slides in from the leading (`start`) or trailing (`end`) edge. Built on the native `<dialog>` element. Inherits native focus trap, Escape-to-close, and top-layer stacking. Use for navigation menus, settings panels, filter trays, and any secondary content where a centered modal or bottom sheet would feel wrong.

## When to use which overlay

| Pattern | Use |
|---------|-----|
| Centered confirmation, address-verification, required decision | `civ-modal` |
| Trigger-anchored popup (filter chip, "more options" on a row) | `civ-action-sheet` |
| Slide-in nav menu, settings panel, side filter tray | `civ-drawer` |

## Examples

### Default

<StoryEmbed id="overlays-drawer--default" minHeight={500} />

### End-anchored (settings panel)

<StoryEmbed id="overlays-drawer--end-anchored" minHeight={500} />

### Mobile main menu

<StoryEmbed id="overlays-drawer--main-menu" minHeight={500} />

### Sticky footer with action buttons

<StoryEmbed id="overlays-drawer--sticky-footer" minHeight={500} />

### Non-sticky header and footer

<StoryEmbed id="overlays-drawer--non-sticky-header-and-footer" minHeight={500} />

### Custom dismiss (no built-in close)

<StoryEmbed id="overlays-drawer--no-close-affordance" minHeight={500} />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/overlays-drawer--default)

## Props

<PropsTable />

## Slots

| Slot | Description |
|------|-------------|
| default | Drawer body content |
| `data-drawer-footer` | Footer area for action buttons (Apply / Cancel / Save). Sticks to the bottom by default; opt out with `no-sticky-footer` |

## Events

<EventsTable />

## Behavior

- **Position:** `start` slides in from the leading edge (left in LTR, right in RTL), `end` from the trailing edge. Uses CSS logical properties so RTL layouts mirror automatically.
- **Height:** Spans the full viewport height regardless of content. Body region scrolls when content overflows.
- **Width:** Controlled by the `width` prop (CSS length). Capped to `100vw` so it never overflows offscreen.
- **Sticky header & footer (default):** Header and footer stick to the top and bottom of the drawer, respectively. Content scrolls between them. Use `no-sticky-header` or `no-sticky-footer` to make either region scroll with the body content instead (e.g. for reading-mode drawers).
- **Focus trap:** Native `<dialog>` behavior. Focus is trapped inside and restored to the trigger on close.
- **Body scroll:** Locked when open, restored on close (preserves the host page's prior `overflow` value rather than clearing it).
- **Mobile (≤480px):** Drawer keeps its side-anchored layout rather than collapsing to a bottom sheet. The bottom-sheet rule applies to trigger-anchored popups (action sheet, combobox-style listboxes); a slide-in nav needs the full viewport height the soft keyboard can't crush.
- **Escape:** Closes by default. Use `no-escape-close` to prevent.
- **Backdrop click:** Closes by default. Use `no-backdrop-close` to prevent.

## Usage

```html
<!-- Slide-in main navigation (mobile) -->
<civ-drawer ?open="${this.navOpen}" position="start" label="Main menu"
  @civ-close="${() => this.navOpen = false}">
  <nav aria-label="Main">
    <civ-link href="/">Home</civ-link>
    <civ-link href="/benefits">Benefits</civ-link>
    <civ-link href="/claims">Claims</civ-link>
  </nav>
</civ-drawer>

<!-- Filter panel with sticky footer action buttons -->
<civ-drawer ?open="${this.filtersOpen}" position="end" heading="Filter results" width="380px"
  @civ-close="${() => this.filtersOpen = false}">
  <!-- Long list of filter checkboxes scrolls between sticky header and footer -->
  <civ-checkbox label="Category A"></civ-checkbox>
  <civ-checkbox label="Category B"></civ-checkbox>

  <div data-drawer-footer>
    <civ-button emphasis="secondary" label="Reset"></civ-button>
    <civ-button label="Apply"></civ-button>
  </div>
</civ-drawer>

<!-- Reading-mode drawer — header scrolls away with content -->
<civ-drawer ?open="${this.articleOpen}" position="end" heading="Article" width="400px"
  no-sticky-header no-sticky-footer>
  <p>Long-form content…</p>
  <div data-drawer-footer>
    <civ-button label="Done"></civ-button>
  </div>
</civ-drawer>
```
