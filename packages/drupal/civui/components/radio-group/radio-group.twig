<civ-radio-group
  {% if legend %}legend="{{ legend }}"{% endif %}
  {% if name %}name="{{ name }}"{% endif %}
  {% if value %}value="{{ value }}"{% endif %}
  {% if required %}required{% endif %}
  {% if disabled %}disabled{% endif %}
  {% if preset %}preset="{{ preset }}"{% endif %}
  {% if preset_variant %}preset-variant="{{ preset_variant }}"{% endif %}
  {% if tile %}tile{% endif %}
  {% if orientation %}orientation="{{ orientation }}"{% endif %}
  {% if variant %}variant="{{ variant }}"{% endif %}
  {% if skip_label %}skip-label="{{ skip_label }}"{% endif %}
  {% if skip_value %}skip-value="{{ skip_value }}"{% endif %}
>
  {% block default %}{{ default }}{% endblock %}
</civ-radio-group>
