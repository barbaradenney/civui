<civ-spinner
  {% if delay %}delay="{{ delay }}"{% endif %}
  {% if min_duration %}min-duration="{{ min_duration }}"{% endif %}
  {% if decorative %}decorative{% endif %}
  {% if size %}size="{{ size }}"{% endif %}
></civ-spinner>
