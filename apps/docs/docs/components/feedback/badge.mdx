---
title: Badge
sidebar_position: 5
sidebar_label: Badge
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_badge.props.mdx";

# Badge

<!-- contract-link:start -->

> **Contract reference:** [`civ-badge`](pathname:///civui/storybook/?path=/docs/contract-civ-badge--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-badge>`

A compact status indicator. Use for state ("Approved", "Pending"), severity, or as a notification dot. Renders `role="status"` so screen readers announce the value.

For **numeric counts** ("5", "99+", or notification overlays like "3 unread"), use [`civ-count`](./count) instead. For categorization (topics, taxonomies), use [`civ-tag`](../layout/tag).

## Examples

### Default

<StoryEmbed id="feedback-badge--default" />

### Status variants

<StoryEmbed id="feedback-badge--status-variants" />

### Dots

<StoryEmbed id="feedback-badge--dots" />

### Styles

<StoryEmbed id="feedback-badge--styles" />

### Spacing

<StoryEmbed id="feedback-badge--spacing" />

### With Icon (auto by variant)

<StoryEmbed id="feedback-badge--with-icon" />

### Explicit Icons (start + end)

<StoryEmbed id="feedback-badge--explicit-icons" />

### Overlay (notification mode)

<StoryEmbed id="feedback-badge--overlay" />

### Badge vs Tag vs Count

<StoryEmbed id="feedback-badge--vs-tag-and-count" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/feedback-badge--default)

## Props

<PropsTable />

## Modes

### Status text

```html
<civ-badge label="Approved" intent="success"></civ-badge>
<civ-badge label="Pending" intent="warning"></civ-badge>
<civ-badge label="Denied" intent="error"></civ-badge>
```

### Dot

```html
<civ-badge dot label="Unread messages" intent="error"></civ-badge>
<!-- label becomes aria-label, no visible text -->
```

## Accessibility

- Text badges and dots-with-label render `role="status"` so screen readers announce the badge value.
- In **dot mode**, `label` becomes the `aria-label`. **If `label` is omitted, the dot is marked `aria-hidden="true"`** (no empty live region). Always set `label` when the dot conveys meaning that isn't already present in a parent's accessible name.
- **Color is never the sole indicator.** Dot mode without a label communicates status via color alone — ~8% of male / 0.5% of female users can't reliably distinguish red-vs-green or amber-vs-green dots at this size (protanopia / deuteranopia, ~1.5% combined). A dot badge with `intent` but no `label` emits a dev-mode warning; either supply `label` (preferred — surfaces the meaning to screen readers too) or drop the intent for a presence-only marker.
- Restricted to semantic colors so visual meaning matches the announced value.

### Notification overlay

Wrap a target (icon, button) with `civ-badge-anchor` to anchor the badge:

```html
<button class="civ-badge-anchor">
  <civ-icon name="mail" size="lg"></civ-icon>
  <civ-badge overlay dot label="New mail" intent="error"></civ-badge>
</button>
```

The `civ-badge-anchor` utility class sets `position: relative; display: inline-block`. Any other relatively-positioned parent works too. For numeric overlays ("3 unread"), use [`civ-count` with `overlay`](./count#overlay) instead.

## Component selection

| Use case | Component |
|----------|-----------|
| Status pill ("Approved", "Pending", "Error") | `civ-badge` |
| Notification dot | `civ-badge` (with `dot`) |
| Numeric count (`5`, `99+`) | [`civ-count`](./count) |
| Notification count over an icon | [`civ-count`](./count) (with `overlay`) |
| Categorization ("Healthcare", "Disability") | [`civ-tag`](../layout/tag) |
| Interactive filter chip | [`civ-filter-chip`](../actions/filter-chip) |

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/badge/`. Include it from any Twig template. The SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:badge' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`badge.component.yml`](pathname:///civui/storybook/?path=/story/feedback-badge-drupal-sdc--default) and the template in [`badge.twig`](pathname:///civui/storybook/?path=/story/feedback-badge-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="feedback-badge-drupal-sdc--default" />

