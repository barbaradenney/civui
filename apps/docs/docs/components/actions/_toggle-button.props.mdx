{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `label` | `string` | `''` | Unpressed label ("Show", "Mute", "Expand") |
| `pressed-label` | `string` | `''` | Pressed label ("Hide", "Unmute", "Collapse"). Required — the swap label gives the toggle its meaning. There is no fallback because the two labels are intent-specific |
| `pressed` | `boolean` | `false` | Reflected boolean state. Two-way bindable; consumers can set this externally to drive the rendered label and ARIA state without going through a click |
| `emphasis` | `'primary'` \| `'secondary'` \| `'tertiary'` | `'secondary'` | Visual emphasis. `secondary` (default) is the gray pill — the common case (helper rows, standalone toggles, password reveal). `primary` is the filled brand pill. `tertiary` is the transparent text-link style for surfaces where the toggle should read as quiet |
| `variant` | `''` \| `'chip'` \| `'inline'` | `''` | **Deprecated** alias for `emphasis`. `variant="chip"` maps to `emphasis="secondary"`; `variant="inline"` maps to `emphasis="tertiary"`. Setting this prop emits a one-time dev warning and overrides `emphasis`. Will be removed in the next major release |
| `icon-start` | `string` | `''` | Optional leading icon name from the CivUI icon library (e.g. `chevron-down` for an accordion expand-all toggle, `visibility` for a password reveal). Rendered with `aria-hidden="true"` so the label remains the accessible name |
