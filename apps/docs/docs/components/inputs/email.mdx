---
title: Email Address
sidebar_position: 14
sidebar_label: Email
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# Email Address

`<civ-email>`

Pre-configured input for email addresses with built-in validation and plain-language labeling.

## Examples

### Default

<StoryEmbed id="inputs-email--default" />

### Custom Label

<StoryEmbed id="inputs-email--custom-label" />

### With Error

<StoryEmbed id="inputs-email--with-error" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/inputs-email--default)

## Shared Props

All form components share these props: `label`, `name`, `value`, `hint`, `error`, `required`, `disabled`, `readonly`.

The label defaults to "Email address" and can be overridden via the `label` prop.

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `civ-input` | `{ value: string }` | Fires on every value change |
| `civ-change` | `{ value: string }` | Fires on committed value change |

## Usage

```html
<!-- Email input with built-in validation -->
<civ-email name="email" required></civ-email>
```
