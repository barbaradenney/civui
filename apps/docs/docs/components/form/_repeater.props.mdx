{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `hint` | `string` | `''` | Hint text shown below the repeater legend |
| `error` | `string` | `''` | Error message shown for the repeater |
| `disabled` | `boolean` | `false` | Disables the repeater — the add / remove controls and all rows become non-interactive |
| `legend` | `string` | `''` | Top-level legend rendered above the repeated instances |
| `heading-level` | `number` | — | When set, promotes the legend to a heading via role="heading" + aria-level=N |
| `size` | `'sm'` \| `'md'` \| `'lg'` \| `'xl'` | — | Visual size of the legend |
| `item-label` | `string` | `'item'` | Singular noun for the repeated thing. Interpolated into "Add another &lcub;item&rcub;" and remove labels |
| `mode` | `'inline'` \| `'form-steps'` \| `'route'` | `'inline'` | Layout mode. `inline` = all rows visible at once; `form-steps` = summary cards with an in-place wizard on add/edit (same page); `route` = summary cards with Add/Edit linking to host-rendered routes (separate page, controlled `rows` prop) |
| `form-steps-sensitive` | `boolean` | `false` | In form-steps mode, treat the repeater as a sensitive flow. Auto-pause / save state on navigation away |
| `form-steps-show-pause` | `boolean` | `false` | In form-steps mode, render an explicit "Save and continue later" affordance on each step |
| `min` | `number` | `0` | Minimum number of instances. Defaults to 0. The repeater renders only the "Add" button until the user clicks to create the first instance. Set higher to render that many rows up front. Also acts as the floor for removal |
| `max` | `number` | `0` | Maximum number of instances. 0 = no limit. Above this, the "Add another" button is hidden |
| `rows` | `array` | `[]` | Host-owned array of row objects. Only used in `mode="route"`. The repeater renders one summary card per entry and never mutates this array |
| `add-href` | `string` | `''` | URL the "Add" affordance links to in `mode="route"`. Required when route mode is used |
| `edit-href-pattern` | `string` | `''` | URL template for the "Edit" link on each row in `mode="route"`. Interpolates `{id}` (from `row[idField]`) and `{index}` (zero-based). Example: `/dependents/{id}/edit` |
| `id-field` | `string` | `'id'` | Name of the row property that holds the stable identifier interpolated into `{id}` in `editHrefPattern` |
| `summary-fields` | `string` | `''` | Comma-separated row property names joined into each summary card. Declarative alternative to the JS `rowSummary` function for non-JS hosts |
| `summary-template` | `string` | `''` | Format string with `{prop}` placeholders for the per-row summary line (e.g. `"{firstName} {lastName} ({relationship})"`). Cross-platform; takes precedence over `summary-fields` when set; loses to `rowSummary` function when both are set |
| `row-summary` | `string` | — | Function `(row: object, index: number) => string` that returns the summary line for each row in `mode="route"`. Set programmatically (not as an attribute). When set, takes precedence over `summary-template` and `summary-fields` |
| `empty-state-text` | `string` | `''` | Hint shown between the header and the Add button when the list has no rows. Leave blank to omit |
