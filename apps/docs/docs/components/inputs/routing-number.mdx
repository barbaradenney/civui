---
title: Routing Number
sidebar_position: 18
sidebar_label: Routing Number
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# Routing Number

`<civ-routing-number>`

Pre-configured input for bank routing numbers with built-in validation and plain-language labeling.

## Examples

### Default

<StoryEmbed id="inputs-routing-number--default" />

### With Error

<StoryEmbed id="inputs-routing-number--with-error" />

### Custom Label

<StoryEmbed id="inputs-routing-number--custom-label" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/inputs-routing-number--default)

## Shared Props

All form components share these props: `label`, `name`, `value`, `hint`, `error`, `required`, `disabled`, `readonly`.

The label defaults to "Routing number" and can be overridden via the `label` prop.

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `civ-input` | `{ value: string }` | Fires on every value change |
| `civ-change` | `{ value: string }` | Fires on committed value change |

## Usage

```html
<!-- Routing number input with built-in validation -->
<civ-routing-number name="routing" required></civ-routing-number>
```
