---
title: Form
sidebar_position: 2
sidebar_label: Form
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# Form

`<civ-form>`

Validation coordinator with accessible error summary. Validates child CivUI form elements on submit, renders an error summary with anchor links to each invalid field, and optionally persists form data to `sessionStorage`.

## Examples

### Default

<StoryEmbed id="forms-form-form--default" />

### With validation

Fields declare client-side rules via `validate="..."` and `required`. The form blocks submit and renders the error summary if anything fails.

<StoryEmbed id="forms-form-form--with-validation" />

### With error summary

Pre-set errors on each field to demonstrate the error-summary layout (heading + anchor list) at the top of the form.

<StoryEmbed id="forms-form-form--with-error" />

### Persist draft

`persist="key"` auto-saves form data to `sessionStorage` on every change and restores it on page reload.

<StoryEmbed id="forms-form-form--persist-draft" />

### Dirty tracking

`track-dirty` raises a `civ-dirty` event when any field is modified and shows a beforeunload warning if the user tries to navigate away.

<StoryEmbed id="forms-form-form--dirty-tracking" />

### All states

<StoryEmbed id="forms-form-form--all-states" />

### Density scale

<StoryEmbed id="forms-form-form--density-scale" />

### Government inquiry form

<StoryEmbed id="forms-form-form--government-inquiry-form" />

### Server error injection

After an async submit fails, call `form.setServerErrors({ fieldName: 'message' })` to populate the error summary and per-field errors. Behaves like a client-validation failure (focuses the summary, announces, anchor links). Pass an empty object to clear.

<StoryEmbed id="forms-form-form--set-server-errors" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-form-form--default)

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `action` | `string` | `''` | Form action URL (for reference; submission is via events) |
| `method` | `'get' \| 'post'` | `'post'` | Form method (for reference) |
| `form-label` | `string` | `''` | Accessible label for the form (sets `aria-label`) |
| `persist` | `string` | `''` | Storage key for auto-saving to sessionStorage |
| `prefill` | `boolean` | `false` | Pre-fill fields from URL query parameters |
| `track-dirty` | `boolean` | `false` | Track whether any field has been modified |
| `error-heading-level` | `3 \| 4 \| 5 \| 6` | `3` | Heading level for the error summary |
| `required-legend` | `boolean` | `false` | Render "&#42; indicates a required field" in the form footer |
| `confirm-before-submit` | `boolean` | `false` | Intercept validated submits and dispatch `civ-submit-confirm` with `proceed()` / `cancel()` callbacks |

## Persistence

When `persist` is set:
- Form data is auto-saved to `sessionStorage` on every field change (debounced 500ms)
- Data is restored on page reload
- Data is cleared on successful submit
- Fields with `data-civ-pii` (SSN/EIN masks) are automatically excluded
- Fields with `data-persist-exclude` are excluded

## Error Summary

On validation failure, an error summary is rendered at the top of the form with:
- A heading: "There is a problem" (1 error) or "There are N problems" (multiple)
- Anchor links to each invalid field
- `role="alert"` for screen reader announcement
- Focus is moved to the summary automatically

## Dirty Tracking

When `track-dirty` is enabled:
- The form tracks initial values of all fields after first render
- `civ-dirty` events fire when dirty state changes
- `beforeunload` warning is shown if the user tries to leave with unsaved changes

## Programmatic API

```js
const form = document.querySelector('civ-form');

form.validate();         // Returns array of errors, sets error on fields
form.clearErrors();      // Clear all field errors and error summary
form.reset();            // Reset all fields to initial values
form.getFormData();      // Returns Record<string, string>
form.toFormData();       // Returns FormData with files and multi-values
form.dirty;             // Whether any field has been modified
```

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `civ-submit` | `{ formData: FormData }` | Fires when validation passes |
| `civ-invalid` | `{ errors: FormFieldError[] }` | Fires when validation fails |
| `civ-dirty` | `{ dirty: boolean }` | Fires when dirty state changes |
| `civ-submit-confirm` | `{ formData, data, proceed, cancel }` | Fires before `civ-submit` when `confirm-before-submit` is set. Call `proceed()` to fire the actual submit; `cancel()` to abort |
| `civ-submit-cancelled` | — | Fires when `cancel()` was called on the matching `civ-submit-confirm` |

## Submission

Submit is triggered by:
- Clicking a `<button type="submit">` (or button with no type) inside the form
- Pressing Enter in a form field

Native form submission is prevented. Handle the `civ-submit` event to process the data.

## Usage

```html
<!-- Basic form with validation -->
<civ-form form-label="Benefits application" @civ-submit="${handleSubmit}">
  <civ-text-input label="First name" name="firstName" required></civ-text-input>
  <civ-text-input label="Last name" name="lastName" required></civ-text-input>
  <civ-text-input label="Email" name="email" type="email" validate="email" required></civ-text-input>
  <button type="submit">Submit</button>
</civ-form>

<!-- With persistence and dirty tracking -->
<civ-form
  persist="my-application"
  track-dirty
  @civ-submit="${handleSubmit}"
  @civ-dirty="${handleDirty}"
>
  <!-- fields -->
  <button type="submit">Submit application</button>
</civ-form>

<!-- With URL prefill -->
<civ-form prefill>
  <!-- Fields will be prefilled from ?firstName=Jane&lastName=Doe -->
  <civ-text-input label="First name" name="firstName"></civ-text-input>
  <civ-text-input label="Last name" name="lastName"></civ-text-input>
</civ-form>
```

## Required-field legend

Federal forms commonly end with a "&#42; indicates a required field"
legend so users know what the asterisk means. Pass the `required-legend`
prop and the legend renders in the form footer:

```html
<civ-form required-legend>
  <civ-text-input label="Full name" name="name" required></civ-text-input>
  <civ-text-input label="Email" name="email"></civ-text-input>
  <civ-button type="submit">Submit</civ-button>
</civ-form>
```

## Statutory disclosures slot

Federal forms typically end with Privacy Act, Paperwork Reduction Act, and
OMB Control Number statements. Pass a child block tagged
`data-civ-form-disclosures` and it relocates into a styled footer slot
beneath the form content, separated by a subtle top border:

```html
<civ-form required-legend>
  <!-- fields and submit button -->

  <div data-civ-form-disclosures>
    <p>
      <strong>Privacy Act Statement:</strong> Authority — 5 U.S.C. § 301
      and Public Law 89-554. Purpose — collect identifying information to
      process your application…
    </p>
    <p>
      <strong>Paperwork Reduction Act:</strong> OMB Control No. 1234-5678.
      Public reporting burden is estimated at 15 minutes per response.
    </p>
  </div>
</civ-form>
```

## Confirm before submit

For irreversible submissions (filing a claim, signing a tax return), add
`confirm-before-submit`. After validation passes, the form dispatches
`civ-submit-confirm` with `proceed()` and `cancel()` callbacks instead of
firing `civ-submit` immediately. The consumer shows their own confirmation
UI — a modal, a confirmation page, or an inline prompt — and decides
whether to proceed:

```html
<civ-form
  confirm-before-submit
  @civ-submit-confirm="${(e) => {
    const ok = window.confirm('File this claim? It cannot be edited after submission.');
    if (ok) e.detail.proceed();
    else e.detail.cancel();
  }}"
  @civ-submit="${handleSubmit}"
  @civ-submit-cancelled="${handleCancelled}"
>
  <civ-text-input label="Claim type" name="type" required></civ-text-input>
  <civ-textarea label="Description" name="desc" required></civ-textarea>
  <civ-button type="submit">File claim</civ-button>
</civ-form>
```

Repeat submit attempts while a confirmation is pending are deduped —
only the first `civ-submit-confirm` is authoritative. A second click
no-ops until the consumer resolves the first one with `proceed()` or
`cancel()`.

## Draft persistence

For multi-session form drafts (benefits applications, tax forms, immigration
forms), use [`<civ-form-autosave>`](./form-autosave.mdx) as a child of
`civ-form`. The autosave element persists in-progress answers to
`localStorage` / `sessionStorage` / a custom adapter and restores them on
the next page load — automatically excluding PII-flagged fields.

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/form/`. Include it from any Twig template — the SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:form' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`form.component.yml`](pathname:///civui/storybook/?path=/story/forms-form-form-drupal-sdc--default) and the template in [`form.twig`](pathname:///civui/storybook/?path=/story/forms-form-form-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-form-form-drupal-sdc--default" />

