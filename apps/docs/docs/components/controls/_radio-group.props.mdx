{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `legend` | `string` | `''` | Group legend text (rendered as `<legend>`). The component is self-contained. Do not wrap in civ-fieldset. |
| `heading-level` | `number` | — | When set, promotes the legend to a heading via role="heading" + aria-level=N |
| `size` | `'sm'` \| `'md'` \| `'lg'` \| `'xl'` | — | Visual size of the legend |
| `tile` | `boolean` | `true` | Tile variant with bordered card styling for child radios. Default true. Tiles are the standard treatment |
| `orientation` | `'vertical'` \| `'horizontal'` | `'vertical'` | Layout orientation for child radio buttons |
| `variant` | `'auto'` \| `'card'` \| `'list'` | `'auto'` | Tile rendering variant. auto picks card for ≤4 options and list for 5+. Vertical + tile only |
| `preset` | `'us-state'` \| `'service-branch'` \| `'discharge-type'` \| `'suffix'` \| `'relationship-type'` \| `'marital-status'` \| `'ethnicity'` \| `'gender'` \| `'language'` \| `'housing-status'` \| `'education-level'` \| `'employment-status'` \| `'income-source'` \| `'veteran-status'` | — | Pre-built option list. Renders `<civ-radio>` children automatically when no slotted children are present |
| `preset-variant` | `string` | — | Variant of the preset (e.g. "territories", "all", "binary") |
| `skip-label` | `string` | `''` | When non-empty, renders a "Prefer not to answer" affordance below the radio group. Outside the roving tabindex |
| `skip-value` | `string` | `'skip'` | Form value used when the skip affordance is selected |
