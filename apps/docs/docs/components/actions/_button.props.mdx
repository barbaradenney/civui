{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `emphasis` | `'primary'` \| `'secondary'` \| `'tertiary'` | `'primary'` | Visual emphasis. `primary` = solid filled (main page action); `secondary` = bordered; `tertiary` = text-only |
| `danger` | `boolean` | `false` | Apply destructive-action styling (red palette). Combine with any variant |
| `type` | `'button'` \| `'submit'` \| `'reset'` | `'button'` | HTML button type. `submit` triggers the enclosing form; `reset` clears it; `button` (default) does neither |
| `icon-start` | `string` | `''` | Leading icon name |
| `icon-end` | `string` | `''` | Trailing icon name |
| `icon-only` | `boolean` | `false` | Render the `label` visually hidden so only the icon is visible; the label still provides the accessible name. Use for square icon buttons such as kebab triggers, close buttons, or compact toolbar actions. Requires at least one of `iconStart` / `iconEnd` to be set |
| `loading` | `boolean` | `false` | Async-in-flight state. Swaps the leading icon for a `civ-spinner`, disables the button, and sets `aria-busy`. Link-mode buttons (`href` set) ignore `loading` because navigation isn't a state we wait on |
| `loading-label` | `string` | `''` | Accessible name applied as `aria-label` on the host while `loading` is true, and announced once via `@civui/core`'s shared `announce()` queue on the loading transition. Empty default falls back to the locale's `buttonLoadingLabel` ("Loading…" in English). Pass an action-specific present-participle verb ("Saving…", "Submitting your application…") for clarity |
| `href` | `string` | `''` | When set, renders as `<a href>` instead of `<button>`. For navigation affordances that visually belong with other buttons (e.g. an "Add another" link styled as a secondary button). The label is underlined so the link identity stays visible |
| `target` | `string` | `''` | HTML `target` attribute (link mode only). Web-only. Use `new-tab` for the common "open in new tab" case |
| `rel` | `string` | `''` | HTML `rel` attribute (link mode only). Web-only. `new-tab` automatically sets `rel="noopener noreferrer"` |
| `download` | `string` | `''` | HTML `download` attribute (link mode only, suggested filename). Web-only. Native platforms handle downloads via OS share/save sheets |
| `new-tab` | `boolean` | `false` | Open the link in a new tab/window (link mode only). Sets `target="_blank"` + `rel="noopener noreferrer"`. Web-only. Native platforms route URLs through the OS, which has no "new tab" concept |
| `disabled` | `boolean` | `false` | Disabled state — the button is inert and suppresses click + analytics events. In link mode (`href` set), renders `aria-disabled="true"` on the rendered `<a>` |
