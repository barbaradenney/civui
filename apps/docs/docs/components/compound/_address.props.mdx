{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand — run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `variant` | `'default'` \| `'general-delivery'` \| `'contact'` | `'default'` | Layout variant. default = standard postal mail address; general-delivery = relaxed structure for unhoused / general-delivery addresses; contact = single-line contact-card layout |
| `legend` | `string` | `''` | Top-level legend rendered above the sub-fields |
| `heading-level` | `number` | — | When set, promotes the legend to a heading via role="heading" + aria-level=N. Use sparingly |
| `size` | `'sm'` \| `'md'` \| `'lg'` \| `'xl'` | — | Visual size of the legend. md/lg/xl step up through the typography scale for use as a section/page heading |
| `show-street2` | `boolean` | `true` | Render the optional second address line |
| `show-street3` | `boolean` | `false` | Render the optional third address line |
| `show-country` | `boolean` | `false` | Render the country selector. Defaults off — most government forms collect US-only addresses |
| `street-error` | `string` | `''` | Per-field error for the street line, surfaced via the inner civ-text-input |
| `city-error` | `string` | `''` | Per-field error for the city input |
| `state-error` | `string` | `''` | Per-field error for the state select |
| `zip-error` | `string` | `''` | Per-field error for the ZIP input |
| `validate-address` | `string` | — | Async validator function `(addr: AddressValue) => Promise<{valid: boolean; suggestion?: AddressValue; error?: string}>`. Set programmatically (not as an attribute) — wires into the address-validation flow |
