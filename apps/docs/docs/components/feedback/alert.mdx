---
title: Alert
sidebar_position: 3
sidebar_label: Alert
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_alert.props.mdx";
import EventsTable from "./_alert.events.mdx";

# Alert

<!-- contract-link:start -->

> **Contract reference:** [`civ-alert`](pathname:///civui/storybook/?path=/docs/contract-civ-alert--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-alert>`

An accessible alert component for informational, warning, error, or success messages. Supports a heading, dismissible close button, and slim (compact) variant.

## Examples

### Default

<StoryEmbed id="feedback-alert--default" />

### Info

<StoryEmbed id="feedback-alert--info" />

### Warning

<StoryEmbed id="feedback-alert--warning" />

### Error

<StoryEmbed id="feedback-alert--error" />

### Success

<StoryEmbed id="feedback-alert--success" />

### All Variants

<StoryEmbed id="feedback-alert--all-variants" />

### Primary Style

<StoryEmbed id="feedback-alert--primary-style" />

### Tertiary Style

<StoryEmbed id="feedback-alert--tertiary-style" />

### Slim

<StoryEmbed id="feedback-alert--slim" />

### Dismissible

<StoryEmbed id="feedback-alert--dismissible" />

### Density Scale

<StoryEmbed id="feedback-alert--density-scale" />

### Government Form Validation

<StoryEmbed id="feedback-alert--government-form-validation" />

### Compact

<StoryEmbed id="feedback-alert--compact" />

### Collapsible (accordion) alert

Set `collapsible` with a `heading` to wrap the body in a native `<details>`/`<summary>` disclosure. The heading becomes the toggle.

<StoryEmbed id="feedback-alert--collapsible" />

### Collapsible — initially open

<StoryEmbed id="feedback-alert--collapsible-open" />

### Collapsible + dismissible

Chevron toggle and close button coexist on the heading row. The dismiss click stops propagation so it doesn't also toggle the disclosure.

<StoryEmbed id="feedback-alert--collapsible-dismissible" />

### Full-width site banner

Set `full-width` for persistent site-wide notices. ARIA role switches from `alert`/`status` (live region) to `region` (landmark), `aria-label` is auto-derived from the heading, and the inner content centers to `--civ-site-max-width` so the banner spans the viewport edge-to-edge while text stays readable. Place as the first child of `<body>` or your outermost site wrapper.

<StoryEmbed id="feedback-alert--full-width-banner" />

### Full-width site banner — dismissible

<StoryEmbed id="feedback-alert--full-width-banner-dismissible" />

### Composed with civ-notice

`civ-alert` accepts rich children when `label` is unset. Compose a `civ-notice` inside the alert body to pair the banner chrome (background, ARIA live region, dismiss) with icon-prefixed emphasis text.

<StoryEmbed id="feedback-alert--composed-with-notice" />

### Composed with multiple civ-notice rows

Stack several `civ-notice` rows inside one alert body when listing layered consequences. Each row carries its own icon + bold emphasis so the points read independently.

<StoryEmbed id="feedback-alert--composed-with-multiple-notices" />

### Composed with civ-badge — status summary

A status badge inside the alert surfaces the current state at a glance. The badge sits on its own line above the body text so it reads as a label for the explanation that follows.

<StoryEmbed id="feedback-alert--composed-with-badge" />

### Composed with multiple civ-badge — multi-step status

A short list of badges inside the alert body labels each sub-item with the variant that matches its real state. Colorblind users get the meaning from the icon + label, not just color.

<StoryEmbed id="feedback-alert--composed-with-badge-list" />

### Composed rich body — badge + notice + descriptive copy

Real placements mix multiple primitives in one alert body: a leading status badge (the answer), a paragraph of context (the explanation), and a trailing `civ-notice` (the emphasis the user must not miss).

<StoryEmbed id="feedback-alert--composed-rich-body" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/feedback-alert--default)

## Props

<PropsTable />

## Events

<EventsTable />

## Accessibility

- `variant="error"` sets `role="alert"` (assertive announcement)
- All other variants set `role="status"` (polite announcement)
- Heading uses `role="heading"` with appropriate `aria-level`
- Dismissal announces a confirmation message to screen readers

## Usage

### Info alert

```html
<civ-alert
  intent="info"
  heading="Scheduled maintenance"
  label="The system will be unavailable Saturday from 2:00 AM to 6:00 AM ET."
></civ-alert>
```

### Error alert

```html
<civ-alert
  intent="error"
  heading="There is a problem"
  label="Your session has expired. Please sign in again to continue."
></civ-alert>
```

### Success alert

```html
<civ-alert
  intent="success"
  heading="Application submitted"
  label="Your claim has been received. You will get a confirmation email within 24 hours."
></civ-alert>
```

### Warning alert

```html
<civ-alert
  intent="warning"
  heading="Unsaved changes"
  label="You have unsaved changes. If you leave this page, your progress will be lost."
></civ-alert>
```

### Dismissible alert

```html
<civ-alert
  intent="info"
  heading="New feature available"
  label="You can now upload documents directly from your phone."
  dismissible
></civ-alert>
```

### Slim (compact) variant

```html
<civ-alert intent="success" slim label="Your changes have been saved."></civ-alert>
```

### Primary style (high emphasis)

```html
<civ-alert
  intent="error"
  emphasis="primary"
  heading="Service unavailable"
  label="We are experiencing technical difficulties. Please try again later."
></civ-alert>
```

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/alert/`. Include it from any Twig template. The SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:alert' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`alert.component.yml`](pathname:///civui/storybook/?path=/story/feedback-alert-drupal-sdc--default) and the template in [`alert.twig`](pathname:///civui/storybook/?path=/story/feedback-alert-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="feedback-alert-drupal-sdc--default" />

