---
title: Race & Ethnicity
sidebar_position: 4
sidebar_label: Race & Ethnicity
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_race-ethnicity.props.mdx";
import EventsTable from "./_race-ethnicity.events.mdx";

# Race & Ethnicity

<!-- contract-link:start -->

> **Contract reference:** [`civ-race-ethnicity`](pathname:///civui/storybook/?path=/docs/contract-civ-race-ethnicity--docs) — auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-race-ethnicity>`

Compound component implementing the OMB (Office of Management and Budget) standard for collecting race and ethnicity data on federal forms. Renders ethnicity as a radio group (single-select) and race as a checkbox group (multi-select).

Ethnicity and race are collected separately because individuals may identify with multiple races but select one ethnicity category. This is the standard pattern required on federal forms.

## Examples

### Default

<StoryEmbed id="forms-compound-race-ethnicity--default" />

### Custom Section Legends

<StoryEmbed id="forms-compound-race-ethnicity--with-custom-legends" />

### Prefilled

<StoryEmbed id="forms-compound-race-ethnicity--prefilled" />

### Disabled

<StoryEmbed id="forms-compound-race-ethnicity--disabled" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-compound-race-ethnicity--default)

## Shared Props

All form components share these props: `label`, `name`, `value`, `hint`, `error`, `required`, `disabled`.

## Component-Specific Props

<PropsTable />

## Value Structure

The component stores its value as a JSON string:

```typescript
interface RaceEthnicityValue {
  ethnicity: string;   // Selected ethnicity option value
  race: string[];      // Array of selected race option values
}
```

## Ethnicity Options

| Value | Label |
|-------|-------|
| `hispanic-latino` | Hispanic or Latino |
| `not-hispanic-latino` | Not Hispanic or Latino |
| `prefer-not-to-answer` | Prefer not to answer |

## Race Options

| Value | Label |
|-------|-------|
| `american-indian-alaska-native` | American Indian or Alaska Native |
| `asian` | Asian |
| `black-african-american` | Black or African American |
| `native-hawaiian-pacific-islander` | Native Hawaiian or Other Pacific Islander |
| `white` | White |
| `prefer-not-to-answer` | Prefer not to answer |

## Events

<EventsTable />

## Usage

```html
<!-- Basic usage -->
<civ-race-ethnicity
  legend="What is your race, ethnicity, or origin?"
  name="demographics"
  required
></civ-race-ethnicity>

<!-- With custom sub-legends -->
<civ-race-ethnicity
  legend="Demographic information"
  ethnicity-legend="What is your ethnicity?"
  race-legend="What is your race?"
  name="demographics"
  required
></civ-race-ethnicity>
```

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/race-ethnicity/`. Include it from any Twig template — the SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:race-ethnicity' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`race-ethnicity.component.yml`](pathname:///civui/storybook/?path=/story/forms-compound-race-ethnicity-drupal-sdc--default) and the template in [`race-ethnicity.twig`](pathname:///civui/storybook/?path=/story/forms-compound-race-ethnicity-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-compound-race-ethnicity-drupal-sdc--default" />

