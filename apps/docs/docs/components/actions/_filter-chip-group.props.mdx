{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `name` | `string` | `''` | Identifier used by composition parents (e.g. `civ-filterable-list`) to bind this group's selection to a filter column. NOT a form-field name — the group is not form-associated |
| `label` | `string` | `''` | Accessible name for the wrapper (`aria-label`). **Strongly recommended.** The wrapper carries `role="toolbar"` in multi mode or `role="radiogroup"` in single mode; both roles require a name per WAI-ARIA. Omit only when the surrounding context already names the group |
| `mode` | `'multi'` \| `'single'` | `'multi'` | Selection mode. `multi` = any subset can be selected; `single` = radio-like, one at a time |
