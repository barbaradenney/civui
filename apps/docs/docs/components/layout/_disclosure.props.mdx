{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand — run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `label` | `string` | `''` | Trigger text. When empty, falls back to the locale-aware default (English: "Why we ask?"). Override via the `t()` registry or set explicitly. |
| `open` | `boolean` | `false` | Whether the disclosure content is currently visible |
| `icon` | `string` | `'chevron-right'` | Icon name from the civ-icon library shown before the label. Default `chevron-right` mirrors the native `<details>` disclosure-triangle pattern — caret points right when collapsed, rotates 90° to point down when expanded. Pass an empty string to suppress. |
| `size` | `'default'` \| `'sm'` | `'default'` | Trigger text size |
