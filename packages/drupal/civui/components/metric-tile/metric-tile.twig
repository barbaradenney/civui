<civ-metric-tile
  {% if unit %}unit="{{ unit }}"{% endif %}
  {% if icon %}icon="{{ icon }}"{% endif %}
  {% if description %}description="{{ description }}"{% endif %}
  {% if delta %}delta="{{ delta }}"{% endif %}
  {% if trend %}trend="{{ trend }}"{% endif %}
  {% if intent %}intent="{{ intent }}"{% endif %}
></civ-metric-tile>
