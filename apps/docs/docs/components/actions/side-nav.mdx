---
title: Side Nav
sidebar_position: 13
sidebar_label: Side Nav
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import SideNavProps from "./_side-nav.props.mdx";
import SideNavItemProps from "./_side-nav-item.props.mdx";
import SideNavItemEvents from "./_side-nav-item.events.mdx";

# Side Nav

<!-- contract-link:start -->

> **Contract reference:** [`civ-side-nav`](pathname:///civui/storybook/?path=/docs/contract-civ-side-nav--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-side-nav>` + `<civ-side-nav-item>`

Vertical, hierarchical navigation — typically a left-rail in documentation pages, admin layouts, or any view with a stable secondary navigation. The parent renders a `<nav>` landmark with a vertical `<ul>` of `<civ-side-nav-item>` rows.

Each row renders in one of two modes, chosen automatically by the presence of nested children:

- **Leaf row** (no nested children) — renders as `<a href>`. The active page sets `current` and that row gets `aria-current="page"` plus a leading-edge primary-color accent rail.
- **Parent / section row** (has nested children) — renders as a `<button aria-expanded>` disclosure with a leading chevron caret that rotates 90° on open. Click anywhere on the row toggles. Parents are disclosure-only; setting `href` on a parent with children is ignored.

On first paint, any parent containing a descendant with `current` is automatically expanded so the active page is visible in the rail. After that, the user (or the consumer, via the `open` prop) drives expand state. Each parent owns its own `open` — opening one section does not collapse others (multi-expand by default).

## Examples

### Default — flat list of leaf links

<StoryEmbed id="navigation-side-nav--default" />

### Nested — auto-expands the active section

<StoryEmbed id="navigation-side-nav--nested" />

### Multiple sections open

<StoryEmbed id="navigation-side-nav--multiple-sections-open" />

### All sections collapsed

<StoryEmbed id="navigation-side-nav--collapsed-by-default" />

### With disabled rows

<StoryEmbed id="navigation-side-nav--with-disabled" />

### Documentation layout

<StoryEmbed id="navigation-side-nav--documentation-layout" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/navigation-side-nav--default)

## Usage

```html
<civ-side-nav label="Documentation">
  <civ-side-nav-item href="/intro" label="Introduction"></civ-side-nav-item>
  <civ-side-nav-item label="Components">
    <civ-side-nav-item href="/components/button" label="Button" current></civ-side-nav-item>
    <civ-side-nav-item href="/components/input" label="Input"></civ-side-nav-item>
  </civ-side-nav-item>
  <civ-side-nav-item href="/tokens" label="Design tokens"></civ-side-nav-item>
</civ-side-nav>
```

The **Components** parent has no `href` — it's automatically a disclosure because it has nested children. The `current` flag goes on the **leaf** (Button), and the parent auto-expands so the active page is visible.

## Props. Civ-side-nav

<SideNavProps />

## Props. Civ-side-nav-item

<SideNavItemProps />

## Events. Civ-side-nav-item

<SideNavItemEvents />

## Mobile

The side-nav stays as a vertical list at every breakpoint — it does **not** collapse into a drawer or hamburger automatically. On small screens, the consumer is responsible for the mobile presentation. Two typical patterns:

- Wrap the rail in `<civ-drawer>` and surface a toggle button on mobile only.
- Render the rail conditionally with a CSS media query (`@media (max-width: 480px)`) and provide a separate top-of-page nav for small screens.

## Dynamic children

`<civ-side-nav-item>` uses the `LightDomSlotMixin` to relocate nested items into the sublist. Per [common-traps.md](https://github.com/barbaradenney/civui/blob/main/.claude/rules/common-traps.md#lightdomslotmixin-composition-with-dynamic-lit-children), rendering items via `${items.map(...)}` directly inside a `<civ-side-nav-item>` can break on outer re-renders. If your nav structure is dynamic, wrap the dynamic items in a static container (a plain `<ul>` won't work since the host expects nested `<civ-side-nav-item>`s, so the cleanest workaround is to render the *whole side-nav* via `keyed()` when the structure changes).

## Accessibility

- Renders as a `<nav>` landmark. Always set `label` when a page contains more than one nav so screen-reader landmark navigation can distinguish them.
- Each link inherits the global focus ring from `civ.css`.
- The active item uses `aria-current="page"` so the user's location is announced.
- Disabled items render as a non-interactive `<span>` with `aria-disabled="true"`. A `disabled` `<a>` would still be focusable, so we drop the href entirely.
- Click activations dispatch `civ-analytics` from the host so consumer-level tracking can listen to navigation events.

## When to use a different component

- For top-of-page primary navigation (horizontal), use `<civ-nav>`.
- For tab-style switching between views in the same context, use `<civ-tabs>`.
- For in-page anchor navigation following the reader's scroll position, use `<civ-on-this-page>`.
