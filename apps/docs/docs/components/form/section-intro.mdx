---
title: Section Intro
sidebar_position: 12
sidebar_label: Section Intro
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_section-intro.props.mdx";

# Section Intro

<!-- contract-link:start -->

> **Contract reference:** [`civ-section-intro`](pathname:///civui/storybook/?path=/docs/contract-civ-section-intro--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-section-intro>`

Pre-section context panel displayed before a sensitive or complex form section. Sets expectations. *"the next questions ask about..."*. So users are not ambushed by difficult content.

Use this at the top of a form step (or any section) that asks personal, clinical, or emotionally heavy questions. It's a presentational primitive: no form state, no validation, no events.

## When to use it

- Before a section asking about trauma, health conditions, bereavement, or service-connected events.
- Before sections with unusual or bureaucratic phrasing that needs plain-language framing.
- Before optional sections, to clarify *"you can skip this"* up front.

Pair it with `civ-form-step` marked `sensitive` and with a `support-resources` footer on `civ-form` for a complete trauma-informed flow.

## Examples

### Default

<StoryEmbed id="forms-form-section-intro--default" />

### Sensitive (trauma-informed)

<StoryEmbed id="forms-form-section-intro--sensitive" />

### With list

<StoryEmbed id="forms-form-section-intro--with-list" />

### Neutral. Body-only

<StoryEmbed id="forms-form-section-intro--neutral-no-heading" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-form-section-intro--default)

## Callout chrome

`civ-section-intro` composes [`civ-callout`](../feedback/callout) internally for its left-border accent. For a generic notice without a heading or section landmark, use `<civ-callout>` directly:

```html
<civ-callout variant="warning">
  <p>Your session will expire in 15 minutes. Save your progress.</p>
</civ-callout>
```

## Props

<PropsTable />

## Accessibility

- Renders a `role="region"` landmark.
- When `heading` is set, the region is labelled via `aria-labelledby` pointing at the heading's generated ID.
- When `heading` is empty, the region falls back to `aria-label="Section introduction"` (overridable via locale).
- Body content is rendered in Light DOM so links, lists, and paragraphs are announced normally.

## Usage

```html
<!-- Basic context panel -->
<civ-section-intro heading="Before we continue">
  <p>The next few questions ask about your service history.</p>
</civ-section-intro>

<!-- Trauma-informed section opener -->
<civ-section-intro
  heading="About your service-connected trauma"
  tone="sensitive"
>
  <p>The next questions ask about events that may be difficult to remember.</p>
  <p>You can skip any question, and your answers are saved as you go.</p>
</civ-section-intro>

<!-- Explaining what to have on hand -->
<civ-section-intro heading="What you'll need" tone="info">
  <p>To finish this section, have the following handy:</p>
  <ul>
    <li>The deceased person's Social Security number</li>
    <li>Their date of birth and date of death</li>
  </ul>
</civ-section-intro>

<!-- Body-only (no heading) for a short inline note -->
<civ-section-intro tone="neutral">
  <p>This section is optional. You can skip it and come back later.</p>
</civ-section-intro>
```

## Related

- `civ-form-step`: set `sensitive` to soften step entry and enable pause/resume.
- `civ-form`: `support-resources` renders a persistent crisis/helpline footer.
- `civ-yes-no`, `civ-radio-group`. `skip-label` adds a "Prefer not to answer" affordance.

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/section-intro/`. Include it from any Twig template. The SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:section-intro' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`section-intro.component.yml`](pathname:///civui/storybook/?path=/story/forms-form-section-intro-drupal-sdc--default) and the template in [`section-intro.twig`](pathname:///civui/storybook/?path=/story/forms-form-section-intro-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-form-section-intro-drupal-sdc--default" />

