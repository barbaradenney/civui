{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand — run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `expanded` | `boolean` | `false` | Whether the rest region is currently visible |
| `more-label` | `string` | `''` | Override the "Read more" trigger text. When empty, falls back to the locale-aware `readMoreButton` string (English: "Read more"). |
| `less-label` | `string` | `''` | Override the "Read less" trigger text. When empty, falls back to the locale-aware `readLessButton` string (English: "Read less"). |
| `icon` | `string` | `''` | Optional icon name from the civ-icon library shown before the label. Empty by default — the trigger text alone is the affordance. Pass e.g. `chevron-down` for a 180° rotate-on-expand visual cue. |
| `size` | `'default'` \| `'sm'` | `'default'` | Trigger text size |
