---
title: Textarea
sidebar_position: 2
sidebar_label: Textarea
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_textarea.props.mdx";
import EventsTable from "./_textarea.events.mdx";

# Textarea

<!-- contract-link:start -->

> **Contract reference:** [`civ-textarea`](pathname:///civui/storybook/?path=/docs/contract-civ-textarea--docs) — auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-textarea>`

Multi-line text input with optional character count and word count display. Uses ElementInternals for native form participation.

## Examples

### Default

<StoryEmbed id="forms-inputs-textarea--default" />

### With Hint

<StoryEmbed id="forms-inputs-textarea--with-hint" />

### With Error

<StoryEmbed id="forms-inputs-textarea--with-error" />

### Required

<StoryEmbed id="forms-inputs-textarea--required" />

### Disabled

<StoryEmbed id="forms-inputs-textarea--disabled" />

### With Character Count

<StoryEmbed id="forms-inputs-textarea--with-character-count" />

### With Word Count

<StoryEmbed id="forms-inputs-textarea--with-word-count" />

### All States

<StoryEmbed id="forms-inputs-textarea--all-states" />

### Density Scale

<StoryEmbed id="forms-inputs-textarea--density-scale" />

### Government Appeal Form

<StoryEmbed id="forms-inputs-textarea--government-appeal-form" />

### Validate Length

Set `validate="length"` to run length validation on blur against `minlength` / `maxlength`. Other text-input validators (email, ssn, phone) intentionally aren't accepted on textarea — they don't fit multi-line content.

<StoryEmbed id="forms-inputs-textarea--validate-length" />
<StoryEmbed id="forms-inputs-textarea--validate-min-length" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-inputs-textarea--default)

## Shared Props

All form components share these props: `label`, `name`, `value`, `hint`, `error`, `required`, `disabled`.

## Component-Specific Props

<PropsTable />

## Character Count

When `maxlength` is set, a character count indicator appears below the textarea showing remaining characters. The count updates visually on every keystroke, but screen reader announcements are debounced (1 second) to avoid spamming.

## Word Count

When `maxwords` is set (and `maxlength` is not), a word count indicator appears. If the user exceeds the limit, an error state is shown automatically.

## Events

<EventsTable />

## Usage

```html
<!-- Basic textarea -->
<civ-textarea
  label="Describe your symptoms"
  name="symptoms"
  hint="Include when they started and how often they occur."
  required
></civ-textarea>

<!-- With character count -->
<civ-textarea
  label="Additional comments"
  name="comments"
  maxlength="500"
  hint="You have 500 characters available."
></civ-textarea>

<!-- With word count -->
<civ-textarea
  label="Personal statement"
  name="statement"
  maxwords="250"
></civ-textarea>

<!-- Auto-growing textarea -->
<civ-textarea
  label="Notes"
  name="notes"
  rows="3"
  autogrow
  max-height="400px"
></civ-textarea>
```

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/textarea/`. Include it from any Twig template — the SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:textarea' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`textarea.component.yml`](pathname:///civui/storybook/?path=/story/forms-inputs-textarea-drupal-sdc--default) and the template in [`textarea.twig`](pathname:///civui/storybook/?path=/story/forms-inputs-textarea-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-inputs-textarea-drupal-sdc--default" />

