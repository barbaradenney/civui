---
title: Icon
sidebar_position: 1
sidebar_label: Icon
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# Icon

`<civ-icon>`

A pure CSS icon component. Each icon is rendered as a single `<span>` with the shape drawn entirely via `::before`/`::after` pseudo-elements. No SVG, no font files, no Unicode characters -- just CSS.

Icons inherit `color` and scale with `font-size`, so they automatically match surrounding text.

## Examples

### Default

<StoryEmbed id="core-icon--default" />

### With Label

<StoryEmbed id="core-icon--with-label" />

### Sizes

<StoryEmbed id="core-icon--sizes" />

### Inherit Color

<StoryEmbed id="core-icon--inherit-color" />

### Navigation Icons

<StoryEmbed id="core-icon--navigation" />

### Action Icons

<StoryEmbed id="core-icon--actions" />

### Status Icons

<StoryEmbed id="core-icon--status" />

### All Icons

<StoryEmbed id="core-icon--all-icons" />

### Inline With Text

<StoryEmbed id="core-icon--inline-with-text" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/core-icon--default)

### Live CSS Editor

A built-in Storybook playground for editing existing icons or authoring new ones. Pick an icon, edit its `::before` / `::after` rules, and watch the preview update live. See the [Icons foundations page](../../foundations/icons#authoring-icons) for the full workflow.

[Open the icon editor →](pathname:///civui/storybook/?path=/story/core-icon-editor--editor)

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | `''` | The icon name from the built-in library |
| `label` | `string` | `''` | Accessible label. When set, renders `role="img"` + `aria-label`. When omitted, renders `aria-hidden="true"` (decorative). |
| `size` | `string` | `''` | Size shorthand: `sm` (0.75em), `md` (1em), `lg` (1.5em), `xl` (2em), `2xl` (3em), or any CSS length |
| `rotate` | `number` | — | Rotation in degrees (e.g., 90, 180, 270) |
| `flip` | `string` | — | Flip direction: `'horizontal'`, `'vertical'`, or `'both'` |

## Built-In Icons

CivUI ships only the 14 icons used by its own components:

- **Navigation:** `chevron-left`, `chevron-right`, `chevron-down`, `external-link`
- **Status:** `check`, `check-circle`, `error`, `warning`, `info`, `loading`
- **Actions:** `close`, `download`
- **Communication:** `mail`, `phone`

For anything outside this set, register your own CSS shape with `registerIcon()` or opt in to the [Material Symbols font](../../foundations/icons#beyond-the-built-in-set).

## Accessibility

- When `label` is set, the icon renders with `role="img"` and `aria-label` for screen readers.
- When `label` is omitted, the icon is decorative and renders with `aria-hidden="true"`.
- Always set `label` on icons that convey meaning (e.g., status indicators, action buttons).

## Usage

```html
<!-- Decorative icon (hidden from screen readers) -->
<civ-icon name="check-circle"></civ-icon>

<!-- Meaningful icon with accessible label -->
<civ-icon name="error" label="Error"></civ-icon>

<!-- Sized and colored -->
<civ-icon name="mail" size="lg" class="civ-text-primary"></civ-icon>

<!-- Rotated -->
<civ-icon name="chevron-right" rotate="90"></civ-icon>

<!-- Inline with text -->
<p><civ-icon name="info" size="sm"></civ-icon> Additional information available</p>
```
