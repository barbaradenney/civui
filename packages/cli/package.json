{
  "name": "@civui/cli",
  "version": "0.1.0",
  "description": "CivUI developer CLI — generate, build, test, validate, release",
  "license": "MIT",
  "type": "module",
  "bin": {
    "civ": "./dist/index.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
