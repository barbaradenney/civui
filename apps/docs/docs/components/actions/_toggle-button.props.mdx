{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand — run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `label` | `string` | `''` | Unpressed label ("Show", "Mute", "Expand") |
| `pressed-label` | `string` | `''` | Pressed label ("Hide", "Unmute", "Collapse"). Required — the swap label gives the toggle its meaning. There is no fallback because the two labels are intent-specific |
| `pressed` | `boolean` | `false` | Reflected boolean state. Two-way bindable; consumers can set this externally to drive the rendered label and ARIA state without going through a click |
| `variant` | `'chip'` \| `'inline'` | `'chip'` | Picks the text-btn modifier. `chip` (default) is the prominent gray pill — the common case (helper rows, standalone toggles, password reveal). `inline` is the transparent text-link style for surfaces where the toggle should read as quiet |
