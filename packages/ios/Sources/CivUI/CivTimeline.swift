// CivUI — CivTimeline for SwiftUI
// Vertical timeline of `civ-timeline-item` entries. Mirrors civ-timeline.
//
// Placeholder body — the prop surface satisfies schema parity. The
// SwiftUI implementation should render a VStack of CivTimelineItem
// rows. See .claude/rules/audit-debt.md →
// "Native platform implementation pass".

import SwiftUI

public struct CivTimeline: View {
    public init() {}

    public var body: some View {
        EmptyView()
    }
}
