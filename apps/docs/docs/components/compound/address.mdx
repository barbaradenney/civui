---
title: Address
sidebar_position: 3
sidebar_label: Address
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_address.props.mdx";
import EventsTable from "./_address.events.mdx";

# Address

<!-- contract-link:start -->

> **Contract reference:** [`civ-address`](pathname:///civui/storybook/?path=/docs/contract-civ-address--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-address>`

Structured address input with country selection, street, city, state, and ZIP/postal code fields. Adapts automatically based on the selected country. US addresses show a state dropdown (including military codes AA/AE/AP), other countries show a text input for state/province, and countries without states or postal codes hide those fields entirely.

## Examples

### Default

<StoryEmbed id="forms-compound-address--default" />

### With Errors

<StoryEmbed id="forms-compound-address--with-errors" />

### Required

<StoryEmbed id="forms-compound-address--required" />

### Disabled

<StoryEmbed id="forms-compound-address--disabled" />

### All States

<StoryEmbed id="forms-compound-address--all-states" />

### Density Scale

<StoryEmbed id="forms-compound-address--density-scale" />

### Prefilled

<StoryEmbed id="forms-compound-address--prefilled" />

### Full Options

<StoryEmbed id="forms-compound-address--full-options" />

### Government Mailing Form

<StoryEmbed id="forms-compound-address--government-mailing-form" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-compound-address--default)

## Shared Props

All form components share these props: `name`, `value`, `hint`, `error`, `required`, `disabled`.

## Component-Specific Props

<PropsTable />

## Variants

### Default

The standard full address form with country, street, city, state, and ZIP/postal code.

### General Delivery (`variant="general-delivery"`)

For USPS General Delivery addresses. Renders the same fields as the default variant but adds a hint explaining how to use General Delivery (enter the post office city/state/ZIP, use "General Delivery" as the street address).

### Contact (`variant="contact"`)

For situations where someone cannot receive mail. Renders a simplified form with just city/state and a contact method textarea (phone, email, shelter name, or someone who can relay messages).

## Value Structure

The component stores its value as a JSON string:

```typescript
interface AddressValue {
  country: string;   // ISO 3166-1 alpha-2 code (e.g., 'US', 'GB', 'JP')
  street1: string;
  street2: string;
  street3: string;
  city: string;
  state: string;     // 2-letter code for US, free text for others
  zip: string;
}
```

## Country-Adaptive Fields

The form adapts based on the selected country:

| Country | State field | Postal code | Labels |
|---------|-----------|-------------|--------|
| **United States** | Select dropdown (50 states + territories + AA/AE/AP) | Shown | "State", "ZIP code" |
| **UK, Canada, etc.** | Text input | Shown | "State/Province/Region", "Postal code" |
| **Hong Kong, Singapore, etc.** | Hidden (no states) | Hidden (no postal codes) |. |

Military addresses use the standard form. Select AA, AE, or AP from the State dropdown and enter APO, FPO, or DPO as the city.

## Sub-field Autocomplete

Fields automatically set appropriate `autocomplete` values:
- Country: `country`
- Street: `address-line1`, `address-line2`, `address-line3`
- City: `address-level2`
- State: `address-level1`
- ZIP: `postal-code`

## Events

<EventsTable />

## Usage

```html
<!-- Standard address -->
<civ-address
  legend="Mailing address"
  name="mailing"
  required
></civ-address>

<!-- With extra street line -->
<civ-address
  legend="Complete address"
  name="fullAddr"
  show-street3
  required
></civ-address>

<!-- Minimal (no street2) -->
<civ-address
  legend="Home address"
  name="home"
  hide-street2="false"
  required
></civ-address>
```

## Related patterns

- [Housing](/form-patterns/housing). Patterns for collecting addresses from people who may not have permanent housing, using address variants and composed components

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/address/`. Include it from any Twig template. The SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:address' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`address.component.yml`](pathname:///civui/storybook/?path=/story/forms-compound-address-drupal-sdc--default) and the template in [`address.twig`](pathname:///civui/storybook/?path=/story/forms-compound-address-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-compound-address-drupal-sdc--default" />

