---
title: Count
sidebar_position: 6
sidebar_label: Count
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_count.props.mdx";

# Count

<!-- contract-link:start -->

> **Contract reference:** [`civ-count`](pathname:///civui/storybook/?path=/docs/contract-civ-count--docs). Auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-count>`

A numeric annotation. Match counts on filter chips, item counts in navigation, notification counters. Lighter chrome than [`civ-badge`](./badge) so it composes inside other components without competing visually.

For status text or dot indicators use [`civ-badge`](./badge). For categorization labels use [`civ-tag`](../layout/tag).

## Examples

### Default

<StoryEmbed id="feedback-count--default" />

### Styles

<StoryEmbed id="feedback-count--styles" />

### Spacing

<StoryEmbed id="feedback-count--spacing" />

### Overflow

<StoryEmbed id="feedback-count--overflow" />

### Notification overlay

<StoryEmbed id="feedback-count--notification-overlay" />

### Inline in chips

<StoryEmbed id="feedback-count--inline-in-chips" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/feedback-count--default)

## Props

<PropsTable />

## Modes

### Inline (default. Secondary style)

Bare colored text that inherits its parent's color. Sits inside chips, list items, navigation links without adding chrome.

```html
<span>Inbox <civ-count count="12"></civ-count></span>
<civ-filter-chip label="Healthcare" count="24"></civ-filter-chip>
```

### Pill (primary style)

Filled rounded pill. For standalone counters or notification badges.

```html
<civ-count count="5" intent="info" emphasis="primary"></civ-count>
<civ-count count="150" max="99" intent="error" emphasis="primary"></civ-count>
```

### Overlay

Absolute-positioned over a relative parent. Pair with [`civ-badge-anchor`](./badge#notification-overlay) for the icon-with-counter pattern:

```html
<button class="civ-badge-anchor">
  <civ-icon name="mail" size="lg"></civ-icon>
  <civ-count overlay count="3" intent="error" emphasis="primary" live="polite"></civ-count>
</button>
```

## Accessibility

- Counts are static annotations by default (`live="off"`), so they don't add live-region noise to nearby labels.
- Set `live="polite"` for **inbox-style counters** that change as data arrives. Screen readers will announce "Inbox 13" when the value updates.
- Set `live="assertive"` for urgent updates (use sparingly).
- The numeric value uses `font-variant-numeric: tabular-nums` so digit widths align across renders.

## Component selection

| Use case | Component |
|----------|-----------|
| Match count in a filter chip | `civ-count` (used internally by `civ-filter-chip`) |
| Inbox / mailbox counter (changes over time) | `civ-count` with `live="polite"` |
| Notification badge over an icon | `civ-count` with `overlay` and `count-style="primary"` |
| Static status pill ("Approved") | [`civ-badge`](./badge) |
| Notification dot (no number) | [`civ-badge`](./badge) with `dot` |
| Categorization label | [`civ-tag`](../layout/tag) |

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/count/`. Include it from any Twig template. The SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:count' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`count.component.yml`](pathname:///civui/storybook/?path=/story/feedback-count-drupal-sdc--default) and the template in [`count.twig`](pathname:///civui/storybook/?path=/story/feedback-count-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="feedback-count-drupal-sdc--default" />

