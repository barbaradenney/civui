---
title: Summary
sidebar_position: 4
sidebar_label: Summary
---

import StoryEmbed from "@site/src/components/StoryEmbed";
import PropsTable from "./_summary.props.mdx";
import EventsTable from "./_summary.events.mdx";

# Summary

<!-- contract-link:start -->

> **Contract reference:** [`civ-summary`](pathname:///civui/storybook/?path=/docs/contract-civ-summary--docs) — auto-generated structured contract for cross-platform implementation.

<!-- contract-link:end -->

`<civ-summary>`

Read-only review page component that displays form data before final submission. Renders structured sections with headings, edit links, and key-value pairs.

## Examples

### Default

<StoryEmbed id="forms-form-summary--default" />

### With Missing Values

<StoryEmbed id="forms-form-summary--with-missing-values" />

### Without Edit Links

<StoryEmbed id="forms-form-summary--without-edit-links" />

### With Array Values

Multi-line values like a mailing address render as a stacked list inside the value cell.

<StoryEmbed id="forms-form-summary--with-array-values" />

### Empty State

When `sections` is empty, the summary renders an unobtrusive empty-state placeholder.

<StoryEmbed id="forms-form-summary--empty-state" />

### All States

<StoryEmbed id="forms-form-summary--all-states" />

### Density Scale

<StoryEmbed id="forms-form-summary--density-scale" />

### Government Application Review

<StoryEmbed id="forms-form-summary--government-application-review" />

### Intercept Edit Clicks (civ-edit event)

For SPAs that handle routing internally, listen for the cancelable `civ-edit` event. The detail includes the section, the per-item (when applicable), and the href. Calling `preventDefault()` suppresses the default `<a>` navigation.

<StoryEmbed id="forms-form-summary--intercept-edit-with-civ-edit-event" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-form-summary--default)

## Props

<PropsTable />

## Types

```typescript
interface SummarySection {
  heading: string;        // Section heading (e.g., "Personal information")
  editHref?: string;      // Link for the edit action (href or step ID)
  items: SummaryItem[];   // Key-value pairs to display
}

interface SummaryItem {
  label: string;                  // Label (e.g., "First name")
  value?: string | string[];      // Value(s) to display; falsy renders "Not provided"
}
```

## Events

<EventsTable />

## Usage

```html
<civ-summary heading="Review your information"></civ-summary>

<script>
  const summary = document.querySelector('civ-summary');
  summary.sections = [
    {
      heading: 'Personal information',
      editHref: '#step-1',
      items: [
        { label: 'First name', value: 'Jane' },
        { label: 'Middle name' },
        { label: 'Last name', value: 'Doe' },
        { label: 'Date of birth', value: 'January 15, 1985' },
      ],
    },
    {
      heading: 'Contact information',
      editHref: '#step-2',
      items: [
        { label: 'Email', value: 'jane.doe@example.com' },
        { label: 'Phone', value: '(555) 123-4567' },
        { label: 'Preferred contact methods', value: ['Email', 'Text message'] },
      ],
    },
  ];
</script>
```

## Drupal SDC

This component is also published as a [Drupal Single Directory Component (SDC)](https://www.drupal.org/docs/develop/theming-drupal/using-single-directory-components) at `packages/drupal/civui/components/summary/`. Include it from any Twig template — the SDC emits the underlying `<civ-*>` markup, and CivUI's JavaScript upgrades it on the client:

```twig
{% include 'civui:summary' with {
  // props mirror the Component-Specific Props table above
} only %}
```

The component schema lives in [`summary.component.yml`](pathname:///civui/storybook/?path=/story/forms-form-summary-drupal-sdc--default) and the template in [`summary.twig`](pathname:///civui/storybook/?path=/story/forms-form-summary-drupal-sdc--default). The story below renders the SDC through Storybook's Twig plugin so you can see the exact output:

<StoryEmbed id="forms-form-summary-drupal-sdc--default" />

