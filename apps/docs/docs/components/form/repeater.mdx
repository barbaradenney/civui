---
title: Repeater
sidebar_position: 3
sidebar_label: Repeater
---

import StoryEmbed from "@site/src/components/StoryEmbed";

# Repeater

`<civ-repeater>`

"Add another" pattern for repeating form sections. Wraps a template of form fields and lets users add/remove instances dynamically. Field names are automatically indexed (e.g., `dependents[0].firstName`, `dependents[1].firstName`).

## Examples

### Default

<StoryEmbed id="forms-form-repeater--default" />

### With Hint

<StoryEmbed id="forms-form-repeater--with-hint" />

### With Error

<StoryEmbed id="forms-form-repeater--with-error" />

### Required

<StoryEmbed id="forms-form-repeater--required" />

### Disabled

<StoryEmbed id="forms-form-repeater--disabled" />

### All States

<StoryEmbed id="forms-form-repeater--all-states" />

### Density Scale

<StoryEmbed id="forms-form-repeater--density-scale" />

### Min Max

<StoryEmbed id="forms-form-repeater--min-max" />

### Multiple Fields

<StoryEmbed id="forms-form-repeater--multiple-fields" />

### Government Service Periods

<StoryEmbed id="forms-form-repeater--government-service-periods" />

### Form Steps Mode

<StoryEmbed id="forms-form-repeater--form-steps-mode" />

### Form Steps: Dependents

<StoryEmbed id="forms-form-repeater--form-steps-with-relationship" />

### Form Steps: Max 3 Items

<StoryEmbed id="forms-form-repeater--form-steps-with-max" />

[Open in Storybook →](pathname:///civui/storybook/?path=/story/forms-form-repeater--default)

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `legend` | `string` | `''` | Fieldset legend |
| `name` | `string` | `''` | Base name for indexed form fields |
| `item-label` | `string` | `'item'` | Human-readable label for each item |
| `mode` | `'inline' \| 'form-steps'` | `'inline'` | Display mode |
| `hint` | `string` | `''` | Hint text below the legend |
| `error` | `string` | `''` | Error text |
| `required` | `boolean` | `false` | Whether at least one row is required |
| `disabled` | `boolean` | `false` | Disable the component |
| `min` | `number` | `1` | Minimum number of rows |
| `max` | `number` | `0` | Maximum number of rows (0 = unlimited) |
| `form-steps-sensitive` | `boolean` | `false` | Mark the form-steps sub-flow as emotionally sensitive (form-steps mode only) |
| `form-steps-show-pause` | `boolean` | `false` | Show "Save and come back later" in the form-steps sub-flow (form-steps mode only) |
| `heading-level` | `1`–`6` | — | Promote the legend to a heading via `role="heading"` + `aria-level=N`. See [Heading hierarchy](/docs/components/form/form-group#heading-hierarchy). |
| `size` | `'sm' \| 'md' \| 'lg' \| 'xl'` | `'sm'` | Visual size. `sm` = body size; `md`/`lg`/`xl` step up for section/page heading use. |

## Modes

### Inline Mode (default)

All rows and their fields are visible on the page simultaneously. Best for simple items with 1-3 fields.

### Form Steps Mode

Full-page takeover for complex items. Clicking "Add" hides the list and opens a multi-step `civ-form-step` flow. Each step is defined using `data-step-label` attributes on the template children — the same pattern as `civ-form-step`. Completing the steps saves the item and returns to the summary list. Best for items with multiple sections that benefit from step-by-step guidance (e.g., dependents with name, relationship, and address steps).

## Template Definition

The component captures its initial children as a clonable template. Each row is a clone with indexed names:

```html
<civ-repeater legend="Dependents" name="dependents" item-label="dependent" min="1" max="10">
  <civ-text-input label="First name" name="firstName"></civ-text-input>
  <civ-text-input label="Last name" name="lastName"></civ-text-input>
</civ-repeater>
```

This produces fields named `dependents[0].firstName`, `dependents[0].lastName`, etc.

## Programmatic API

```js
const repeater = document.querySelector('civ-repeater');

repeater.rowCount;        // Current number of rows
repeater.addRow();        // Add a new row
repeater.removeRow(1);    // Remove row at index 1
```

## Events

| Event | Detail | Description |
|-------|--------|-------------|
| `civ-repeater-add` | `{ index: number }` | When a row is added |
| `civ-repeater-remove` | `{ index: number }` | When a row is removed |
| `civ-repeater-form-steps-open` | `{ index: number, isNew: boolean }` | When the form-steps flow opens (form-steps mode) |
| `civ-repeater-form-steps-close` | `{ index: number, action: 'save' \| 'cancel' }` | When the form-steps flow closes (form-steps mode) |

## Usage

```html
<!-- Inline mode (simple items) -->
<civ-repeater
  legend="Previous addresses"
  name="addresses"
  item-label="address"
  min="1"
  max="5"
>
  <civ-text-input label="Street address" name="street" required></civ-text-input>
  <civ-text-input label="City" name="city" required></civ-text-input>
</civ-repeater>

<!-- Form-steps mode (multi-step items) -->
<civ-repeater
  legend="Your dependents"
  name="dependents"
  item-label="dependent"
  mode="form-steps"
  max="10"
>
  <div data-step-label="Name">
    <civ-name legend="Dependent's name" name="name" required></civ-name>
  </div>
  <div data-step-label="Relationship">
    <civ-relationship
      legend="Relationship details"
      name="rel"
      preset="dependent"
      show-name="false"
      required
    ></civ-relationship>
  </div>
</civ-repeater>
```
