{/* Generated from @civui/schema by tools/sync-doc-tables.ts.
    Do not edit by hand. Run `pnpm sync:doc-tables` to regenerate. */}

| Attribute | Type | Default | Description |
|-----------|------|---------|-------------|
| `legend` | `string` | `''` | Top-level legend rendered above the sub-fields |
| `heading-level` | `number` | — | When set, promotes the legend to a heading via role="heading" + aria-level=N |
| `size` | `'sm'` \| `'md'` \| `'lg'` \| `'xl'` | — | Visual size of the legend |
| `preset` | `'general'` \| `'beneficiary'` \| `'dependent'` \| `'survivor'` | `'general'` | Built-in relationship-type preset. Determines which options appear in the relationship selector and which date fields are conditionally rendered |
| `options` | `array` | — | Custom relationship-type configuration. When set, overrides the preset. Web: typed array of `{value, label, fields?}` objects (set as JS property, attribute: false). Native: passed as a JSON-encoded string for serialization. Conceptually the same data, different wire format per platform |
| `show-name` | `boolean` | `true` | Render the relative-name input. Defaults to true. **HTML-boolean trap:** `show-name="false"` does NOT disable it (Lit treats any present boolean attribute as truthy). Use `hide-name` or bind the property directly in lit-html with `.showName=${false}`. |
| `hide-name` | `boolean` | `false` | Inverse of `show-name`. Present in HTML disables the inner name fields. Use when an enclosing repeater step already captures the name and you want to avoid duplicating it inside civ-relationship. |
| `show-deceased` | `boolean` | `false` | Render the "Is this person deceased?" toggle. When true, shows the date-of-death input |
| `deceased-assumed` | `boolean` | `false` | Suppress the deceased toggle and treat all entries as deceased. Used for survivor-only contexts |
| `show-divorce-date` | `boolean` | `false` | Render the divorce-date input when the relationship type is spouse-related |
| `show-adoption-date` | `boolean` | `false` | Render the adoption-date input when the relationship type is child-related |
| `name-error` | `string` | `''` | Compound-level error for the relative-name section |
| `first-error` | `string` | `''` | Per-field error for the first-name input inside the relative-name section |
| `middle-error` | `string` | `''` | Per-field error for the middle-name input inside the relative-name section |
| `last-error` | `string` | `''` | Per-field error for the last-name input inside the relative-name section |
| `relationship-error` | `string` | `''` | Per-field error for the relationship-type selector |
| `marriage-date-error` | `string` | `''` | Per-field error for the marriage-date input |
| `divorce-date-error` | `string` | `''` | Per-field error for the divorce-date input |
| `date-of-birth-error` | `string` | `''` | Per-field error for the date-of-birth input |
| `adoption-date-error` | `string` | `''` | Per-field error for the adoption-date input |
| `date-of-death-error` | `string` | `''` | Per-field error for the date-of-death input |
| `other-description-error` | `string` | `''` | Per-field error for the "describe the relationship" free-text input (shown when relationship = other) |
